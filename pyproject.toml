[project]
name = "py-obsidianmd"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "python-frontmatter>=1.3.0",
]

[dependency-groups]
dev = [
    "icecream>=2.2.0",
    "pytest>=9.1.1",
]
